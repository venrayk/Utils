function Sign(){
    this.deepCopy = function(source){ 
        var result={};
        for (var key in source) {
          result[key] = typeof source[key]==='object'? deepCoyp(source[key]): source[key];
        }
        return result; 
    },
    this.getSign = function(signData){

        var str3=[];

        for (var i in signData) {
            if(i!="key"){
                str3.push(i+"="+signData[i]);
            }
        }

        var atrArr=str3.sort().push("key="+signData.key);

        var strx= str3.join("&");

        for (var i =0; i<4; i++) {
            strx= md5(strx).toUpperCase();
        }

    
        var newObj=this.deepCopy(signData);


        newObj.sign=strx;
        
        return newObj;
    }
}